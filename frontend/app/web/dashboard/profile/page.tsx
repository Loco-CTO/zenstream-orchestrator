"use client";
import { FormEvent, useEffect, useState } from "react";
import { adminFetch, readSession, saveSession, Session } from "../components/admin-client";
export default function ProfilePage() { const [session, setSession] = useState<Session | null>(null); const [username, setUsername] = useState(""); const [password, setPassword] = useState(""); const [message, setMessage] = useState(""); useEffect(() => { const current = readSession(); setSession(current); if (current) setUsername(current.username); }, []);
  async function submit(event: FormEvent) { event.preventDefault(); if (!session) return; const r = await adminFetch("/api/admin/profile", session, { method: "PATCH", headers: { ...(username !== session.username ? { "New-Username": username } : {}), ...(password ? { "New-Password": password } : {}) } }); if (!r.ok) { setMessage((await r.json()).message || "Could not update profile."); return; } const data = await r.json(); const next = { username: data.username, token: session.token }; saveSession(next); setSession(next); setPassword(""); setMessage("Profile updated successfully."); }
  return <div><p className="text-sm text-violet-200/70">Account</p><h1 className="mt-2 text-4xl font-black">Profile & security</h1><form onSubmit={submit} className="mt-8 max-w-xl rounded-2xl border border-white/10 bg-black/25 p-6 backdrop-blur-xl"><h2 className="text-lg font-semibold">Administrator credentials</h2><p className="mt-2 text-sm leading-6 text-white/45">Update the local account used to access this console. Your current session will stay active.</p><label className="mt-6 block text-sm text-white/65">Username<input required value={username} onChange={e => setUsername(e.target.value)} className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-white/5 px-4 outline-none focus:border-violet-300/60" /></label><label className="mt-4 block text-sm text-white/65">New password<input minLength={8} type="password" value={password} onChange={e => setPassword(e.target.value)} placeholder="Leave blank to keep current password" className="mt-2 h-11 w-full rounded-xl border border-white/10 bg-white/5 px-4 outline-none placeholder:text-white/30 focus:border-violet-300/60" /></label><button className="mt-6 rounded-xl bg-violet-300 px-4 py-3 text-sm font-semibold text-black">Save changes</button>{message && <p className="mt-4 text-sm text-violet-200">{message}</p>}</form></div>; }
